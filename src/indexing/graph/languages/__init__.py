"""Per-language adapters for tree-sitter extraction.

Active in the MVP (Phase 5):
    typescript.py — universal extractor for ts/tsx/js/jsx/cjs/mjs
    vue.py        — Vue SFC: parse + extract <script> + injection tree-sitter-typescript

Deferred (post-MVP):
    python.py     — planned, to be implemented once a Python repo shows up
    php.py        — planned
    go.py         — planned
"""
